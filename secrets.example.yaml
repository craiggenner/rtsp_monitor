# Secrets file — referenced from config.yaml via ${VAR_NAME} placeholders.
# Do NOT commit this file to git.

FRONT_DOOR_USER: "admin"
FRONT_DOOR_PASS: "password123"
DRIVEWAY_USER: "admin"
DRIVEWAY_PASS: "secret456"
